
f = novo Func3D();

f.x1 = -pi;
f.x2 = pi;
f.y1 = -pi;                       // valor inferior do intervalo de y setado para -pi
f.y2 = pi;                        // valor posterior do intervalo de y setado para pi

f.fun = func( x, y ) {
    retorne 3*sen( x ) + 8*cos( y );
};

pc = novo PC3D();
pc.graficos[] = f;

plot3d( pc );
